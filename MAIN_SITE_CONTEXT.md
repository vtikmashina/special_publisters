# Контекст основного сайта спецпроектов Паблистеров

Этот файл описывает основной лендинг `https://special.publisters.ru/`.

## Назначение

Основной сайт - интерактивный промо-лендинг отдела спецпроектов Паблистеров. Его задача: показать подход команды, набор инструментов, примеры механик и быстро вывести человека к контакту или сборке проекта.

Ключевая идея страницы: спецпроекты Паблистеров - это не статичная презентация, а живая веб-упаковка с движением, ладошкой-проводником, интерактивными блоками и ощущением креативной лаборатории.

## Где лежит проект

- Локально: `/Users/igorermolenko/Documents/Выгруза пптх/site-repo`
- GitHub: `git@github.com:vtikmashina/special_publisters.git`
- Продакшн: `https://special.publisters.ru/`
- Сервер: `193.53.126.63`
- Корень сайта на сервере: `/var/www/special.publisters.ru`
- Nginx-конфиг в репозитории: `deploy/special.publisters.ru.nginx`

Сайт статический: HTML, CSS, JS, локальные шрифты и изображения. Сборщика нет.

## Главные файлы

- `index.html` - структура основного лендинга.
- `styles.css` - вся визуальная система, адаптив, анимации и состояния.
- `script.js` - интерактив: конструктор брифа, кейсы, ладошка-проводник, орбитальные инструменты, мини-игра.
- `assets/fonts/` - локальные шрифты.
- `assets/images/palm-guide.png` - фирменная ладошка, используется как проводник и персонаж игры.
- `favicon.ico` и `assets/icons/palm-icon-*.png` - favicon и touch-иконки сайта, собраны из фирменной ладошки.
- `assets/images/margarita-mityakina.jpg`, `igor-ermolenko.jpg`, `timofey-vakhrushev.jpg` - аватары в финальном блоке.
- `texts-for-copywriter.md` - выгрузка текстов страницы для сверки копирайтером.
- `research/telegram-channels/` - статический экспорт лендинга-исследования про личные
  Telegram-каналы digital-специалистов.

## Визуальная система

Основной стиль: тёмное стекло, светлая бумажная подложка, крупная сжатая типографика, фиолетовый и лаймовый акценты, мягкие шумы и глубина.

Шрифты:

- `TT Bluescreens` - большие акцидентные заголовки, капс, жирный наклонный стиль.
- `TT Firs Text` - основной текст, навигация, подписи, интерфейсные элементы.

Основные цвета заданы в `:root` в `styles.css`:

- фон: `#f3f1ea`
- тёмные секции: `#17171d`
- фиолетовый: `#7f63ff`
- лаймовый: `#d8ff56`
- светлые карточки: `#fffefa`

Важно: Figma-презентация использовалась как визуальный референс, но сайт не должен быть набором скриншотов из презентации. Если добавлять новые блоки, лучше пересобирать их в веб-стиле лендинга, а не вставлять слайды целиком.

## Дополнительные страницы

### Исследование про Telegram-каналы

URL: `https://special.publisters.ru/research/telegram-channels/`

Это отдельная статическая страница-исследование о том, почему digital-специалисты ведут
личные Telegram-каналы и что меняют блокировки.

Исходный проект:

- GitHub: `https://github.com/dapiskarevada-dot/publishers-landing`
- локальная рабочая копия: `/Users/igorermolenko/Documents/Лендинг с исследованием`

Технически это Next.js-проект со статическим экспортом. Перед переносом в основной сайт
он собирается в `out/`, а содержимое `out/` копируется в `research/telegram-channels/`.
В исходном проекте должен быть настроен `basePath: "/research/telegram-channels"`, а пути
к публичным ассетам, шрифтам и Lottie-анимациям должны начинаться с
`/research/telegram-channels/`, чтобы страница работала из подпапки и не конфликтовала
с корнем основного сайта.

Важные правила после инцидента с мобильной версией 2026-07-01:

- Не копировать экспорт командой `rsync --delete` в `research/telegram-channels/`.
  У Next static export имена CSS/JS чанков хешированные. Если удалить старые чанки, а у
  мобильного браузера останется старый HTML в кеше, страница может перестать открываться.
  Копировать новый `out/` нужно поверх существующей папки без удаления старых файлов:

  ```bash
  rsync -a --chmod=Du=rwx,Dgo=rx,Fu=rw,Fgo=r \
    /Users/igorermolenko/Documents/Лендинг\ с\ исследованием/out/ \
    /Users/igorermolenko/Documents/Выгруза\ пптх/site-repo/research/telegram-channels/
  ```

- После копирования не тащить случайный binary churn: проверить `git status`, убрать
  неиспользуемые ассеты, если они не referenced в HTML/CSS/JS, и нормализовать права шрифтов
  (`chmod 644 research/telegram-channels/fonts/*.woff*`), чтобы не было mode changes.
- Мобильный hero не должен зависеть только от Tailwind 4 utilities внутри cascade layers:
  в исходном `src/app/globals.css` есть fallback-правила для `.hero-mobile` и `.hero-desktop`
  вне `@layer`. Их нельзя удалять без проверки в мобильном WebView.
- Для мобильной hero-картинки важно учитывать retina DPR. Файлы называются
  `image_main-640.webp` и `image_main-960.webp`, но фактически должны быть минимум
  `1280x647` и `1920x970` соответственно, иначе на iPhone картинка выглядит мыльной.
- Перед деплоем обязательно проверить страницу не только на десктопе, но и mobile viewport:
  `390x844`, `deviceScaleFactor: 3`. Минимальная проверка: hero чёрный, картинка загружена,
  `naturalWidth` у мобильной картинки не меньше `1280`, `scrollWidth === innerWidth`,
  нет failed requests.

Проверка перед деплоем:

```bash
cd /Users/igorermolenko/Documents/Лендинг\ с\ исследованием
node node_modules/next/dist/bin/next build

cd /Users/igorermolenko/Documents/Выгруза\ пптх/site-repo
python3 -m http.server 4173
# открыть http://127.0.0.1:4173/research/telegram-channels/
```

## Структура страницы

Ниже описана структура основного лендинга `index.html`.

### 1. Hero

Первый экран с тёмной карточкой, большой ладошкой, CTA-кнопками и плавающими карточками.

Текущий заголовок:

```text
Спецпроекты
Паблистеров
для бренд-историй
```

CTA:

- `Собрать проект` -> `#brief`
- `Посмотреть кейсы` -> `#cases`
- `Выбрать инструменты` -> `#engine`

В hero есть бегущая строка с форматами: боты, mini apps, креативные розыгрыши, ai-механики, wow-посевы, перформансы, ugc.

### 2. Три стула / три пакета

Секция `#stools` объясняет три пакета работы:

- `S` - Чёткий запрос.
- `M` - Задача, решение и реализация.
- `L` - Проактивный креатив под ключ.

В этой секции встроена мини-игра как "непрогруженный кусочек страницы":

- до старта показан серый блок с текстом `Эта часть страницы не прогрузилась.`
- кнопка: `Кликни, чтобы починить`
- после клика запускается игра в духе Chrome Dino
- персонаж - фирменная ладошка в сером варианте
- управление: `Space` или клик/тап по игровому полю
- после проигрыша показывается сообщение: `Вот вам и вовлечение! Придумаем геймификацию и под вашу задачу)))`
- есть кнопка рестарта

### 3. Бриф-конструктор

Секция `#brief` собирает сценарий проекта из двух выборов:

- бизнес-задача
- режим работы / пакет

Данные лежат в `script.js`:

- `goalData`
- `packageModes`
- `briefVariants`

Важно: результаты должны заметно отличаться в зависимости от выбора. Если добавлять новые варианты, нужно менять не только заголовок, но и форматы, шаги, deliverables и общий режим работы.

### 4. Наша машина

Секция `#engine` показывает подход команды и набор инструментов.

В центре: формула `идея + форма + kpi`.

Вокруг двигаются инструменты-баблы:

- чат-бот
- новелла
- розыгрыш
- mini app
- ugc
- видео
- wow-посев
- офлайн
- квест
- квиз
- кликер
- калькулятор
- ai-ролик
- подкаст
- стрим
- репортаж

Поведение:

- баблы плавно движутся по орбитам внутри контейнера
- не должны вылезать за контур блока
- не должны наплывать друг на друга
- при наведении или фокусе показывается краткая расшифровка в tooltip

Логика движения и антиколлизии находится в `script.js` в `animateOrbit()`.

### 5. Кейсы

Секция `#cases` показывает карточки кейсов и активную подробную карточку.

Данные лежат в массиве `cases` в `script.js`.

Текущие кейсы:

- Шашлындос
- Восточные сказки
- Брендированная новелла
- Кредитный бот
- Снеговик, которого можно сломать
- Экспресс-продакшн для марафона

У каждого кейса есть:

- `tag`
- `title`
- `request`
- `idea`
- `result`
- `why`

### 6. Контакты

Финальная секция `#contact` содержит три контакта в таком порядке:

1. Маргарита Митякина
   Операционный директор
   Телефон: `+7 986 731 0259`
   Telegram: `@yamargorobbie`

2. Игорь Ермоленко
   Креативный директор Паблистеров
   Телефон: `+7 (915) 526-87-36`
   Telegram: `@vtikmashina`

3. Тимофей Вахрушев
   Руководитель отдела спецпроектов
   Telegram: `@rfanatic`

Тексты в карточках выровнены по центру под аватарками.

## Сквозные интерактивы

### Ладошка-проводник

`palm-navigator` закреплён справа снизу и сопровождает пользователя по странице. Он показывает короткие подсказки в зависимости от текущей секции и прогресс по странице.

Если ладошка пропадает в проде, первым делом проверить:

- существует ли `assets/images/palm-guide.png`
- корректен ли путь в `index.html`
- не закешировались ли старые `styles.css` или `script.js`
- обновлён ли query-параметр CSS/JS в `index.html`

### Reveal-анимации

Основные секции используют класс `.reveal`. Появление управляется через `IntersectionObserver` в `script.js`.

### Hero parallax

В hero есть элементы с классом `.hero-parallax`, которые слегка реагируют на движение мыши.

### Интерактивные кнопки

Для `.chip`, `.case-card`, `.button` есть pointer-эффект через CSS variables `--x` и `--y`.

## Правила по текстам

Главное ограничение: на ключевых блоках использовать тексты пользователя и копирайтера, не переписывать их нейросетевым стилем.

Чего избегать:

- формулы `Не X, а Y`
- абстрактных фраз без фактуры
- слишком рекламных и общих формулировок
- выдумывания новых смыслов там, где уже есть утверждённый текст

Если текст нужно дописать временно, лучше делать его коротким, предметным и легко заменяемым.

## Правило перед коммитом

Когда пользователь просит закоммитить изменения, сначала проверить, не изменилось ли фактическое
состояние сайта, деплоя, структуры файлов или договорённостей по работе. Если изменилось - обновить
`MAIN_SITE_CONTEXT.md` в этом же коммите, чтобы контекст оставался актуальным для следующих задач.

## Деплой

Обычный порядок:

```bash
cd /Users/igorermolenko/Documents/Выгруза\ пптх/site-repo
git status
git add .
git commit -m "..."
git push
ssh root@193.53.126.63
cd /var/www/special.publisters.ru
git pull
```

Если менялись `styles.css` или `script.js`, желательно обновить query-параметры в `index.html`, чтобы браузер не держал старую версию:

```html
<link rel="stylesheet" href="./styles.css?v=...">
<script src="./script.js?v=..."></script>
```

Nginx настроен на HTTPS через Let's Encrypt и отдаёт статический сайт из `/var/www/special.publisters.ru`.
Для старого раздела отчётов действует отдельное правило:

```nginx
location ^~ /report/ {
    return 410;
}
```

Это нужно, чтобы удалённые страницы `/report/q1/` и `/report/q1-slides/` не открывали главную
страницу через fallback `try_files ... /index.html`.

## Локальный просмотр

```bash
cd /Users/igorermolenko/Documents/Выгруза\ пптх/site-repo
python3 -m http.server 8000
```

Открыть:

```text
http://127.0.0.1:8000/
```

## Что не смешивать с основным сайтом

Отдельные страницы квартального отчёта удалены из репозитория и с продакшна:

- `/report/q1/`
- `/report/q1-slides/`

Старый раздел `/report/` должен возвращать `410 Gone`. Если в будущем появятся новые отчётные
или служебные страницы, их нужно заводить как отдельные продукты и не переносить автоматически
решения в основной лендинг без отдельной задачи.
